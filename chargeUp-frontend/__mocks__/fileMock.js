/**
 * FILE MOCK — for images and static assets
 * Copy this entire __mocks__ folder to chargeUp-frontend/__mocks__/
 * Prevents jest from crashing on image imports (needed on Windows)
 */

module.exports = "test-file-stub";
